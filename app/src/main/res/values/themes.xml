<?xml version="1.0" encoding="utf-8"?>
<resources>
    <style name="Theme.SchwiftySavings" parent="android:Theme.Material.Light.NoActionBar">
        <!-- Avoid black flash before Compose draws (edge-to-edge uses transparent bars) -->
        <item name="android:windowBackground">@color/mint</item>
    </style>
</resources>